<!-- TabScore2, a wireless bridge scoring program.  Copyright(C) 2024 by Peter Flippant
     Licensed under the Apache License, Version 2.0; you may not use this file except in compliance with the License -->

@model TabScore2.Models.EnterContract

<div class="row mt-5">
    <div class="col-12 text-center">
        <h3>
            @Strings["Board"] @Model.BoardNumber
        </h3>
    </div>
</div>

<div class="row">
    <div class="col-12 text-center">
        <h3>
            @Strings["ConfirmResult"]
        </h3>
    </div>
</div>

@if (Model.ContractLevel == 0)
{
    <div class="row mt-4">
        <div class="col-12 text-center">
            <h3>
                <span style="color:darkgreen">@Strings["AllPass"]</span>
            </h3>
        </div>
    </div>
}
else
{
    <div class="row mt-4">
        <div class="col-12 text-center">
            <h3>
                @Html.Raw(Model.ContractDisplay)
            </h3>
        </div>
    </div>

    if (Model.Score > 0)
    {
        <div class="row">
            <div class="col-12 text-center">
                <h3>
                    @Model.Score @Strings["To"] @Strings["N"]@Strings["S"]
                </h3>
            </div>
        </div>
    }
    else
    {
        <div class="row">
            <div class="col-12 text-center">
                <h3>
                    @(-Model.Score) @Strings["To"] @Strings["E"]@Strings["W"]
                </h3>
            </div>
        </div>
    }
}

<script>
    var isSubmitted = false;

    function OKButtonClick() {
        if (document.getElementById("OKButton").disabled) return;
        if (!isSubmitted) {
            isSubmitted = true;
            location.href = '@Url.Action("OKButtonClick", "ConfirmResult")?deviceNumber=@Model.TabletDeviceNumber';
        }
    }

    function BackButtonClick() {
        if (document.getElementById("BackButton").disabled) return;
        if (!isSubmitted) {
            isSubmitted = true;
            location.href = '@Url.Action("BackButtonClick", "ConfirmResult")?deviceNumber=@Model.TabletDeviceNumber';
        }
    }
</script>